import { Mustmatch.Validator } from './mustmatch.validator';

describe('Mustmatch.Validator', () => {
  it('should create an instance', () => {
    expect(new Mustmatch.Validator()).toBeTruthy();
  });
});
